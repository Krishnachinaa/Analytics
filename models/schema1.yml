version: 2

models:
  - name: ANALYTICS.ORDERS
    columns:
      - name: CUSTOMER_ID
        tests:
          - not_null
          - relationships:
              to: ref('CUSTOMERS')
              field: CUSTOMER_ID